README           
METADATA REGISTRY

The Metadata registry consists of a number of applications.  This code is the main java application code, to get a working repository one also needs a sql database, a triple store and possibly a hadoop  instance.
This code runs under Maven.

